---
apiVersion: v1
kind: Service
metadata:
  name: postgres
  namespace: guidewire-infra
  labels:
    app: postgres
    app.kubernetes.io/part-of: guidewire-poc
spec:
  selector:
    app: postgres
  ports:
    - port: 5432
      targetPort: 5432
      protocol: TCP
      name: postgresql
  type: ClusterIP
