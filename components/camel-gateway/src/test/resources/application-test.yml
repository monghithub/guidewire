server:
  port: 0

spring:
  application:
    name: camel-gateway-test

camel:
  springboot:
    name: camel-gateway-test
    main-run-controller: false

kafka:
  bootstrap-servers: localhost:9092

management:
  endpoints:
    web:
      exposure:
        include: health,info
